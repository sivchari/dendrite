tools:
  # GitHub Releases (tar.gz)
  - name: aquaproj/aqua@v2.39.0
    asset:
      darwin: aqua_{os}_{arch}.tar.gz
      linux: aqua_{os}_{arch}.tar.gz

  # GitHub Releases (per-OS format)
  - name: cli/cli@v2.87.0
    asset:
      darwin: gh_{version}_{os}_{arch}.zip
      linux: gh_{version}_{os}_{arch}.tar.gz
    os_map:
      darwin: macOS
    bins:
      - gh

  # Non-GitHub URL
  - name: google/cloud-sdk@v529.0.0
    url: https://dl.google.com/dl/cloudsdk/channels/rapid/downloads/google-cloud-cli-{version}-darwin-arm.tar.gz
    bins:
      - gcloud

  # Custom version prefix
  - name: golang/go@go1.26.0
    asset:
      darwin: go{version}.{os}-{arch}.tar.gz
      linux: go{version}.{os}-{arch}.tar.gz
    version_prefix: "go"
    bins:
      - go
